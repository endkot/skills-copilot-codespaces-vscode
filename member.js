function skillsMember() {
    // 1. Define a variable named `member` and assign it an object
    const member = {
        // 2. Define a key named `name` and assign it the value of your name
        name: 'Sara',
        // 3. Define a key named `age` and assign it the value of your age
        age: 28,
        // 4. Define a key named `skills` and assign it the value of your skills array
        skills: ['HTML', 'CSS', 'JavaScript'],
    };

    // 5. Return the `member` object
    return member;
}